{% extends 'base.html.twig' %}
{% import '_icons.html.twig' as ui %}

{% block title %}Benutzer anlegen · HandwerkCloud{% endblock %}

{% block body %}
    <a class="back-link" href="{{ path('app_user_index') }}">{{ ui.icon('arrow-left', 15) }} Zurück zur Übersicht</a>
    <div class="page-head">
        <div class="page-title">
            <span class="page-title-icon">{{ ui.icon('user-cog', 20) }}</span>
            <div><h1>Benutzer anlegen</h1></div>
        </div>
    </div>
    <div class="card"><div class="card-body">
        {{ include('user/_form.html.twig', {'button_label': 'Benutzer anlegen'}) }}
    </div></div>
{% endblock %}
