# Values used when APP_ENV=test. Doctrine appends "_test" to the database name
# (see config/packages/doctrine.yaml), so tests never touch the dev database.
KERNEL_CLASS='App\Kernel'
APP_SECRET='test_secret_not_used_in_production'
SYMFONY_DEPRECATIONS_HELPER=disabled
